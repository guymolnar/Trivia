<Window x:Class="TriviaClient.CreateRoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TriviaClient"
        mc:Ignorable="d"
        Title="CreateRoomWindow" Height="450" Width="800">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" 
                MaxWidth="350" Margin="20">
                <TextBlock TextAlignment="Center" FontWeight="ExtraBold" FontSize="50" Margin="0,0,0,30">Create Room:</TextBlock>

                <TextBlock FontWeight="Bold" Margin="0,0,0,5">Room Name:</TextBlock>
                <TextBox x:Name="roomName" Margin="0,0,0,15" Padding="5" FontSize="14"/>

                <TextBlock FontWeight="Bold" Margin="0,0,0,5">Time Per Question:</TextBlock>
                <PasswordBox x:Name="timePerQuestion" Margin="0,0,0,15" Padding="5" FontSize="14"/>

                <TextBlock FontWeight="Bold" Margin="0,0,0,5">Maximum Number Of Players::</TextBlock>
                <TextBox x:Name="numOfPlayers" Margin="0,0,0,15" Padding="5" FontSize="14"/>

                <Button x:Name="createRoomBtn" Click="createRoomBtn_click"
                    Padding="10" FontSize="14" Margin="0,0,0,10">Create Room</Button>
                <Button x:Name="backToMenuBtn" Click="backToMenuBtn_click"
    Padding="10" FontSize="14" Margin="0,0,0,10">Back to menu</Button>


                <TextBlock x:Name="txtError" Foreground="Red"
                       TextAlignment="Center" Margin="0,10,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
